ansible-playbook -i inventory.ini deploy-api.yml \
  -e mysql_host=your-mysql-host \
  -e mysql_user=your-mysql-user \
  -e mysql_password=your-mysql-password \
  -e mysql_database=your-mysql-db

# Get connection parameters
MYSQL_HOST=$(terraform output -raw mysql_fqdn)
MYSQL_USER=$(terraform output -raw mysql_admin_user)
MYSQL_DB=$(terraform output -raw mysql_database_name)

# Run Ansible with the gathered parameters
ansible-playbook -i inventory.ini deploy-api.yml \
  -e mysql_host=$MYSQL_HOST \
  -e mysql_user=$MYSQL_USER \
  -e mysql_password=$TF_VAR_admin_password \ # Assuming you set this as a variable
  -e mysql_database=$MYSQL_DB